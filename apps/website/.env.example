NEXT_PUBLIC_SUPABASE_URL=https://anseivwusnyiwopihnqu.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_YR2acnE5k6caiE-ts5xyaA_gtNcV4QR
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Production site URL (used by write-article persist to POST /api/revalidate after --publish)
NEXT_PUBLIC_SITE_URL=https://ludecker.com

# Local dev: browse http://localhost:3001 (Vite). API runs on :3000. Uncomment for local-only persist:
# NEXT_PUBLIC_SITE_URL=http://localhost:3001
# Optional extra revalidate target when NEXT_PUBLIC_SITE_URL is production:
# REVALIDATE_LOCAL_URL=http://localhost:3001

# Shared secret: set the same value on Render and in .env.local (generate with: openssl rand -hex 32)
REVALIDATE_SECRET=your-revalidate-secret
