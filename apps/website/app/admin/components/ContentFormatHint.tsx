export function ContentFormatHint() {
  return (
    <p className="admin-hint">
      Body uses lightweight blocks: <code>C:</code> headings, <code>P1:</code>{" "}
      paragraphs, and predicate lines. Save as draft first, then publish when
      ready.
    </p>
  );
}
