---
description: Render deploy and ship rules for Lüdecker
alwaysApply: false
globs:
  - render.yaml
  - docs/deployment.md
  - .cursor/agents/release-*.md
  - .cursor/skills/shared/platform-release/**
---

# Deploy — Lüdecker

## SSOT

| Fact | Location |
|------|----------|
| Render service name | `ludecker-website` |
| Render MCP workspace | Erik — owner ID `tea-csp7qr3gbbvc73d1fvqg` |
| Blueprint | `render.yaml` at repo root |
| Production smoke URL | `https://ludecker-website.onrender.com/` |
| Deploy flow | Push to `main` → poll Render until `live` → smoke 200 |
| Watch script | `.cursor/skills/shared/platform-release/scripts/watch-render-deploy.mjs` |

## Ship commands

- `/release-app` or `/ship-ludecker` — AAAC release swarm (git → conditional AAAC npm tag → Render)
- AAAC wave runs when `detect-aaac-changes.mjs` reports changes; pushes `aaac-v*` tag for CI publish
- Never force-push `main` unless user explicitly requests
- Never commit `.env`, `.env.local`, or service role keys

## Render MCP

Use **`user-render`** only. Bootstrap workspace if unset: `select_workspace` → `tea-csp7qr3gbbvc73d1fvqg`.

Service id from `list_services` where `name === "ludecker-website"`.

**Mandatory on every ship:** poll deploy until `status === "live"` or report failure with `list_logs`. Never stop at push alone.
