# Decision log — deprecated

**Observability lives on the Run.** Do not append to this file.

Use `state/runs/{run_id}/run.json` → `decisions[]` per [run/schema.json](../run/schema.json) and [telemetry.yaml](telemetry.yaml).
